{
  "name": "sandbox-opensandbox-codex-example",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "prestart": "test -x node_modules/.bin/tsx || pnpm install",
    "prestart:advanced": "test -x node_modules/.bin/tsx || pnpm install",
    "start": "tsx codex-harness-agent.ts",
    "start:advanced": "tsx codex-concurrent-sessions.ts"
  },
  "dependencies": {
    "@ai-sdk/harness": "beta",
    "@ai-sdk/harness-codex": "beta",
    "@natheihei/sandbox-opensandbox": "file:..",
    "tsx": "^4.20.5",
    "typescript": "5.8.3",
    "zod": "3.25.76"
  }
}
